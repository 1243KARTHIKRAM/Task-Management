package com.taskmanagement.backend.repository;

/**
 * Repository layer - Data access layer.
 * 
 * Purpose:
 * - Handles database interactions
 * - Provides CRUD operations via JPA/ORM
 * - Contains database queries (JPQL, Native SQL)
 * - Acts as a bridge between application and database
 * - Extends JpaRepository, CrudRepository, or MongoRepository
 */
public class RepositoryPlaceholder {
    // Repository interfaces use @Repository annotation or extend JpaRepository
    // Example: TaskRepository, UserRepository
}
