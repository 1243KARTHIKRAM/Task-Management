package com.taskmanagement.backend.entity;

/**
 * Entity layer - Domain models / Database tables.
 * 
 * Purpose:
 * - Represents database table mappings (ORM/JPA entities)
 * - Contains @Entity, @Table, @Column annotations
 * - Defines relationships (@OneToMany, @ManyToOne, etc.)
 * - Represents core business objects
 * - Used for database operations and Object-Relational Mapping
 */
public class EntityPlaceholder {
    // Entity classes use @Entity annotation
    // Example: Task, User, Project - correspond to database tables
}
