package com.taskmanagement.backend.security;

/**
 * Security layer - Authentication and Authorization.
 * 
 * Purpose:
 * - Implements Spring Security configuration
 * - Handles authentication (login, token validation)
 * - Manages authorization (role-based access control)
 * - JWT token generation and validation
 * - Password encoding (BCrypt)
 * - Security filters and interceptors
 */
public class SecurityPlaceholder {
    // Security classes handle authentication/authorization
    // Example: JwtAuthenticationFilter, SecurityConfig, UserDetailsService
}